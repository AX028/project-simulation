# Contributing

Use Python 3.11 or newer, install the `dev` extra, and run `pytest`, `ruff check .`, and
`mypy src/project_simulation` before opening a pull request. New content must be deterministic
under a supplied seed and include a focused test.

This repository currently has no license. Opening a contribution does not change that status.
